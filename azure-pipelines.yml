variables:
    - ${{ if eq( variables['Build.SourceBranchName'],'PDD' ) }}:
          - group: CD-Automation-produccion

resources:
    repositories:
        - repository: deploy
          type: git
          name: DevOps/DespliegueAutomatico
          ref: 'develop'

stages:
    - template: azure-pipelines.yml@deploy
